import { RidehistoryPipe } from './ridehistory.pipe';

describe('RidehistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RidehistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
