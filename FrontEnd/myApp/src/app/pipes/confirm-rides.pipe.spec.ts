import { ConfirmRidesPipe } from './confirm-rides.pipe';

describe('ConfirmRidesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmRidesPipe();
    expect(pipe).toBeTruthy();
  });
});
